# configs/config.local.yaml (本地开发)
server:
  port: "8001"
  base_path: "/api/v3"
  environment: "local"

swagger:
  enabled: true
  title: "User Service API"
  version: "3.0.0"
  description: "用户管理服务 v3 - 本地开发环境"
  host: "localhost:8001"
  schemes: ["http"]

database:
  host: "localhost"
  port: 5432
  name: "users_dev"
  user: "postgres"
  password: "postgres"

redis:
  host: "localhost"
  port: 6379
  password: ""
  db: 0
